Segmented control with a sliding cyan thumb — the bottom-sheet tab switcher.

```jsx
<SegmentedTabs tabs={["Generators", "Upgrades"]} active={tab} onChange={setTab} />
```

Labels render uppercase and tracked. The active thumb slides between segments; keep to 2–3 short labels.
