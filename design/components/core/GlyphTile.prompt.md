Rounded tile holding a geometric `Glyph` — the leading element of generator rows and upgrade cards.

```jsx
<GlyphTile name="fusion" active size={44} />
```

`active` gives it a faint cyan tint + cyan inset border; otherwise neutral. The inner glyph auto-sizes to half the tile.
