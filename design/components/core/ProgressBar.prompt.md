Thin cyan progress bar for the Kardashev tier scale.

```jsx
<ProgressBar value={0.42} />
```

`value` is a 0..1 fraction; the fill animates its width. Render it full-width under the hero stat chips.
