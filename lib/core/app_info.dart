/// Название игры. Им же подписаны иконка на телефоне, вкладка браузера,
/// окно на компьютере и превью ссылки — все эти места живут в файлах
/// платформ, куда константу не подставить, и их сверяет
/// `test/app_identity_test.dart`. Раньше в окне Windows так и висело
/// «idle_game» из шаблона, потому что сверять было нечем.
const String kGameTitle = 'Витя в деле';
