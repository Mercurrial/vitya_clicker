<p align="center">
  <img src="web/og.png" alt="Витя в деле — от банки до коллайдера" width="100%">
</p>

<h3 align="center">
  <a href="https://mercurrial.github.io/vitya-v-dele/">▶ Играть в браузере</a>
</h3>

<p align="center">
  Idle-игра на Flutter: Витя гонит самогон в гараже и доходит до орбитального
  самогонного коллайдера.<br>
  Android · iPhone через браузер · Windows · версия 1.0.0 ·
  <a href="CHANGELOG.md">что менялось</a>
</p>

<table align="center">
  <tr>
    <td align="center"><img src="docs/screenshots/garage.png" width="250" alt="Начало: банка в гараже"></td>
    <td align="center"><img src="docs/screenshots/workshop.png" width="250" alt="Середина: четыре аппарата"></td>
    <td align="center"><img src="docs/screenshots/plant.png" width="250" alt="Конец: производство и коллайдер"></td>
  </tr>
  <tr>
    <td align="center"><sub>Банка на кухонном огне</sub></td>
    <td align="center"><sub>Гараж обрастает аппаратами</sub></td>
    <td align="center"><sub>Производство с коллайдером</sub></td>
  </tr>
</table>

<p align="center">
  <a href="#как-поставить">Как поставить</a> ·
  <a href="#как-играть">Как играть</a> ·
  <a href="#разработка">Разработка</a> ·
  <a href="#что-дальше">Что дальше</a>
</p>

---

## Как поставить

### iPhone

Нативная сборка под iOS требует Mac с Xcode. Вместо неё — веб-версия: на
iPhone она ставится штатным способом и работает как обычное приложение.

1. Открыть ссылку в **Safari** (Chrome на iOS так не умеет).
2. «Поделиться» → **«На экран Домой»**.
3. Запускать с иконки. Адресной строки нет, интернет после первого
   открытия не нужен.

Ссылка одна на всех: каждый играет свой прогресс на своём устройстве.

Ставить лучше сразу, до игры. У иконки на экране «Домой» своё хранилище,
отдельное от Safari: прогресс из вкладки туда сам не переедет — только
кодом (вкладка «Витя», ПЕРЕНОС ПРОГРЕССА). А во вкладке Safari iPhone
стирает данные сайта, если 7 дней пользоваться Safari и не заходить в игру;
иконку это не касается. Игра во вкладке Safari на iPhone один раз об этом
напоминает.

### Android

1. Скинуть `app-release.apk` на телефон.
2. Открыть файл, разрешить установку из этого источника.
3. Поставить.

Сборка подписана отладочным ключом. Ключ менять нельзя: иначе следующая
версия не встанет поверх и прогресс потеряется.

### Про сохранения

Прогресс лежит на устройстве — в хранилище браузера или в памяти приложения.
Его можно потерять: очистив данные сайта, сменив телефон, удалив приложение.

На вкладке «Витя» есть **ПЕРЕНОС ПРОГРЕССА**: кнопка сворачивает гараж в
строку, которую можно отправить себе в сообщения, а «Вставить» разворачивает
обратно. Строка защищена контрольной суммой — обрезанный при копировании код
игра не примет молча.

Нечитаемый сейв игра молча не стирает. Сейв более новой версии (на iPhone
браузер бывает открывает старую версию из памяти) не трогается: вместо игры —
просьба обновиться и кнопка перезагрузки. Испорченный сейв откладывается
отдельно, его можно скопировать и прислать разработчику; если следующая
версия сможет его прочитать, она предложит вернуть гараж.

---

## Как играть

| Что | Как работает |
|---|---|
| **ЖАР** | Зажать палец где угодно по гаражу — греется. Отпустить — стынет. На компьютере то же делает пробел. |
| **ОКНО** | Подсвеченный участок шкалы. Пока жар в нём, копится СЕРИЯ. Перегрел — серия сгорает. |
| **СЕРИЯ** | Множитель ко всему производству, до ×3. Не срывается сразу: три секунды держится, потом тает. |
| **СОРТ** | Пять ступеней. Растёт от выдержанного жара, задаёт цену за литр и доступ к покупателям. |
| **БАК** | Копит самогон. Под шкалой — когда он наполнится. Переполнился — производство встало, пора продавать. |
| **ПРОДАТЬ** | Янтарная кнопка наверху. Петрович берёт всё, всегда, по рынку и сорт не трогает. |
| **ГОСТИ** | Приходят рядом с Петровичем ровно раз в 10 минут и стоят 5 минут. Кто придёт — заранее не видно. Платят до ×3.6, но требуют сорт и уносят его с собой. |
| **ЦЕЛИ** | Двадцать целей в пять рядов. Каждая множит производство, закрытый ряд — ещё сильнее, две открывают автопродажу и покупку пачками. |
| **ПОХМЕЛЬЕ** | Заход начинается заново в обмен на мудрость. Мудрость множит производство навсегда. |
| **ВЕХИ** | Мудрость не тратится: каждая N-я сама открывает бонус навсегда — ступень ×2, старт захода с деньгами, улучшения, которые переживают похмелье, с 20-й — всё ×1.5 через одну. Дорожка — на вкладке «МУДРОСТЬ». |
| **КОЛЛАЙДЕР** | Последняя ступень — портал в новый мир, а не просто аппарат: стоит в 50 000 раз дороже, чем по лестнице, и берётся на исходе суток игры. |
| **ПОТОК** | Копится, пока игра закрыта: 10 минут за час, копилка на час. Тратится ускорением до ×10 — аппараты гонят быстрее, а рынок и гости идут по настоящим часам. |

Касание само по себе не даёт ничего: спам по экрану бессмысленен.

Открытая вкладка — это игра, даже скрытая: аппараты гонят, а автопродажа,
если открыта, сдаёт бак. Пока игра закрыта или телефон уснул, аппараты
стоят — зато копится поток.

---

## Разработка

Flutter 3.24.3 / Dart 3.5.3.

```bash
flutter pub get
flutter analyze                                # должно быть пусто
flutter test                                   # всё зелёное
flutter run
```

Порядок работы, правила и проверки — в [CLAUDE.md](CLAUDE.md), решения
владельца — в [docs/DECISIONS.md](docs/DECISIONS.md).

<details>
<summary><b>Замеры и снимки экрана</b></summary>

Замеры по умолчанию пропускаются:

```bash
flutter test --tags perf --run-skipped         # замеры, на спокойной машине
```

**Снимки экрана** (`test/goldens/`) сверяет CI на каждом PR. Эталон снят
на ubuntu в GitHub Actions: на Windows шрифты рисуются иначе, и локально
снимки с текстом не совпадут — это ожидаемо. Переснять: Actions →
«Переснять снимки» → Run workflow → ветка, или

```bash
gh workflow run goldens.yml --ref <ветка>
```

Workflow закоммитит снимки в ту же ветку. **Пересняв — посмотреть
глазами** (в PR, Files changed): снимок, обновлённый не глядя, перестаёт
что-либо проверять.

</details>

<details>
<summary><b>Баланс</b></summary>

```bash
dart run tools/balance_report.dart 4   # прогон 4 часов за трёх игроков
dart run tools/balance_sweep.dart      # перебор чисел под цели
```

Все настраиваемые числа — в `lib/content/balance.dart`. Цели, в которые
экономика обязана попадать, — в `lib/sim/balance_targets.dart`; их стережёт
`test/balance_test.dart`.

</details>

<details>
<summary><b>Сборка и отладка в браузере</b></summary>

```bash
flutter build apk --release      # build/app/outputs/flutter-apk/
flutter build web --release      # build/web
python tools/serve.py            # отдать build/web на localhost:8770
```

Свой сервер, а не `python -m http.server`: он запрещает кэширование и
вычищает service worker. Без этого браузер показывает старую сборку.

Осматривать экран глазами на разных стадиях игры:

```bash
dart run tools/demo_saves.dart   # сейвы начала, середины и конца игры
dart run tools/moments.dart      # когда придут гости
```

Сейв кладётся в `localStorage['flutter.vitya_save']`, а часы страницы
сдвигаются параметром: `http://localhost:8770/?t=<миллисекунды>`. Так гостя
можно увидеть сразу, не дожидаясь их по расписанию.

Скриншоты в начале этой страницы сняты так же: сейвы `early`, `mid` и
`final`, окно 390×844, масштаб 2, жар зажат на 1,2 секунды — ровно в окне.
Перед загрузкой в сейве `lastSeen` ставится на «сейчас», иначе игра
покажет «Пока тебя не было».

</details>

<details>
<summary><b>Графика и звук</b></summary>

```bash
python tools/sprite_lab.py --dart       # аппараты → lib/ui/pixel/still_sprites_gen.dart
python tools/goal_icons_lab.py --dart   # значки целей → lib/ui/pixel/goal_icons_gen.dart
python tools/sprite_lab.py --png out.png  # превью всех аппаратов
dart run tools/make_icons.dart          # иконка на все платформы
python tools/make_banner.py             # баннер: эта страница и превью ссылки
dart run tools/make_sounds.dart         # звуки
```

Пиксель-арт собирается из примитивов с общей светотенью, а не рисуется
строками руками: тринадцать аппаратов и двадцать значков вручную в одном
стиле не выдержать. Сгенерированные файлы руками не правятся — только
функции в лаборатории.

Иконка — Витя строками в `tools/make_icons.dart`. Поле 48×48: обычная
иконка — его середина, запас нужен адаптивной иконке Android и маскируемой
веба. Баннер берёт Витю оттуда же, а аппараты — из `sprite_lab.py`.

</details>

<details>
<summary><b>Устройство проекта</b></summary>

```
lib/
  models/      состояние: ресурсы, аппараты, улучшения, сорт, престиж
  engine/      правила игры — чистые функции, без побочных эффектов
  content/     что в игре есть: аппараты, улучшения, сорта, покупатели, события
  core/        сейвы, миграции, часы, форматирование чисел
  sim/         headless-симулятор и цели баланса
  providers/   Riverpod: склейка движка, времени и интерфейса
  ui/          экран гаража, пиксель-арт, виджеты
```

Четыре правила, на которых всё держится.

**Движок не знает про интерфейс.** `GameEngine` — чистые функции
`состояние → состояние`. Поэтому баланс проверяется тестами за секунды, а не
двадцатиминутным сеансом.

**Формула живёт в одном месте.** Оффлайн-доход однажды разъехался с обычным
тиком и переполнил бак в тысячу раз. Теперь отсутствие вовсе не гонит, а
копит поток времени — и запуск игры, и уснувшее приложение считают его
одним `GameEngine.creditAfk`.

**Миграции сейвов заморожены.** Цепочка в `lib/core/save.dart` пуста до
первого выпуска: игроков ещё нет, и формат сейва правится на месте. После
выпуска 1.0.0 написанный шаг не переписывается никогда, даже под новые
формулы: он обязан читать то, что реально лежит на диске у игрока.
Дублирование кода там намеренное.

**В сейве лежат факты, а не оценки.** Сколько куплено и сколько нагнано —
факты. Доход, цены, мудрость — оценки, и они считаются заново при каждой
загрузке по числам из `lib/content/balance.dart`. Поэтому правка баланса
применяется к старым сейвам сама, без миграции.

Подробнее: [docs/DESIGN.md](docs/DESIGN.md) — почему игра устроена так,
[docs/ROADMAP.md](docs/ROADMAP.md) — состояние работ,
[docs/RELEASE.md](docs/RELEASE.md) — порядок выпуска.

</details>

<details>
<summary><b>Публикация</b></summary>

Один раз в настройках репозитория: **Settings → Pages → Source: GitHub
Actions**.

Сайт обновляется **только выпуском версии** — тегом `vX.Y.Z` на `main`
([.github/workflows/deploy-web.yml](.github/workflows/deploy-web.yml)).
Влитие PR в `main` сайт не трогает. Сборка занимает около двух минут.
Адрес сайта — имя репозитория, выкладка берёт его сама.

Каждый PR до влития проверяется анализом, тестами, снимками экрана и
сборкой веба ([.github/workflows/ci.yml](.github/workflows/ci.yml)). Замеры — руками
перед выпуском; полный порядок в [docs/RELEASE.md](docs/RELEASE.md).

</details>

---

## Что дальше

Полный список с историей — [docs/ROADMAP.md](docs/ROADMAP.md). Выпуск 1.0.0
готовится по [docs/PLAN-1.0.md](docs/PLAN-1.0.md), что в него вошло — в
[CHANGELOG.md](CHANGELOG.md). Здесь то, что после него, по убыванию пользы.

**Потом**

- [ ] **Пуш-уведомления** — «бак полон», «на свадьбе ждут». Нужен плагин и
      запрос разрешения в правильный момент, а не при первом запуске.
- [ ] **Новый мир** — следующий слой престижа: тот же Витя, другое дело,
      коллайдер — портал туда.
- [ ] **Достижения, открывающие механики**, а не только цифры.

**Требует Mac**

- [ ] Нативная сборка под iOS. Веб-версия закрывает потребность, поэтому
      задача не срочная.

**Сознательно не делается**

- Локализация. Игра для своих и по-русски.
- Магазины приложений. Раздача ссылкой и файлом.
- Монетизация.
